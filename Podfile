# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

use_frameworks!

target 'Exercise-for-Elderly' do
  platform :ios, '12.0'

  pod 'Firebase/Core'
  pod 'Firebase/Firestore'

end

target 'ExerciseWatch Watch App' do
  platform :watchos, '7.0'

  # Pods for ExerciseWatch Watch App

end
